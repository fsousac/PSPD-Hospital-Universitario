# Fora da lista `resources` do kustomization.yaml de propósito: aplicar HPA
# desde o início competiria com o scale manual das fases (a)-(c) da
# metodologia (o HPA reverteria `kubectl scale --replicas=3` para o valor que
# ele calcular). Só aplicar na fase (d) (autoscaling):
#   kubectl apply -f k8s/hpa.yaml -n grupo-10
#
# Depende de metrics-server estar instalado no cluster (`kubectl top nodes`
# funcionando) — ver docs/decisions/0005, seção Riscos.
#
# minReplicas: 1 nos 4 — de propósito, para economizar recursos ociosos na
# ResourceQuota compartilhada do namespace (`cota-grupo`: 3 cores request/6
# cores limit). Uma tentativa anterior de fixar minReplicas alto (2-3) por
# serviço para evitar cold-start entre execuções de teste foi revertida: o
# problema real era a forma da carga do k6 (degrau instantâneo 0→N VUs), não
# o piso de réplicas — um degrau não dá ao HPA (~15-30s de sync period) nem
# à réplica nova (até 60s pra ficar `Ready`, JVM) nenhuma janela de reação,
# não importa o mínimo configurado. Corrigido do lado do teste, não do HPA:
# `loadtests/k6-scenario.js` agora sobe de 0 até o alvo de VUs gradualmente
# (`RAMP_UP`, default 30s) antes da fase medida — dá tempo real de reagir e
# a réplica nova ficar pronta antes do pico, sem manter baseline permanente
# (ver docs/decisions/0005).
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-gateway
  namespace: grupo-10
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-gateway
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: authorization-service
  namespace: grupo-10
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: authorization-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: patient-data-service
  namespace: grupo-10
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: patient-data-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: data-transform-service
  namespace: grupo-10
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: data-transform-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
